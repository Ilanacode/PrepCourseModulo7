//function convertirStringAMayusculas(array) {
  // Convierte a mayúsculas todos los strings del array.
  // Retorna el arreglo resultante.
  // Tu código:
//}

//module.exports = convertirStringAMayusculas;
//esto tiene dos implementaciones. el metodo map y el bucle for. pero, yo no sabia como escribir letra.toUpperCase. 
function convertirStringAMayusculas(array){

  console.log(array.map(string => {string.toUpperCase}));
  
}


convertirStringAMayusculas([h,e,n,r,y]);
